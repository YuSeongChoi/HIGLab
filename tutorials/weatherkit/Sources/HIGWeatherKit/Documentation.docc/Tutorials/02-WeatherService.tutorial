@Tutorial(time: 20) {
    @Intro(title: "WeatherService 요청") {
        WeatherService를 사용해 다양한 날씨 데이터를 요청하고 
        에러를 처리하는 방법을 학습합니다.
        
        @Image(source: "weatherservice.png", alt: "WeatherService 요청 흐름")
    }
    
    @Section(title: "WeatherService 기본 사용법") {
        @ContentAndMedia {
            WeatherService는 WeatherKit의 핵심 클래스입니다.
            싱글톤 패턴의 shared 인스턴스를 사용하거나 
            직접 인스턴스를 생성할 수 있습니다.
        }
        
        @Steps {
            @Step {
                WeatherService.shared를 사용해 날씨 데이터를 요청합니다.
                모든 요청은 async/await 기반의 비동기 API입니다.
                
                @Code(name: "WeatherService.swift", file: "02-01-basic.swift")
            }
            
            @Step {
                CLLocation을 사용해 위치를 지정합니다.
                CoreLocation으로 현재 위치를 가져오거나 좌표를 직접 지정할 수 있습니다.
                
                @Code(name: "WeatherService.swift", file: "02-02-location.swift")
            }
            
            @Step {
                weather(for:) 메서드는 Weather 객체를 반환합니다.
                이 객체에는 요청한 모든 날씨 데이터가 포함됩니다.
                
                @Code(name: "WeatherService.swift", file: "02-03-weather.swift")
            }
        }
    }
    
    @Section(title: "DataSet 선택하기") {
        @ContentAndMedia {
            필요한 데이터만 요청하면 API 사용량을 줄이고 
            응답 속도를 높일 수 있습니다.
            
            WeatherKit은 다양한 DataSet을 제공합니다:
            
            - `.current`: 현재 날씨
            - `.hourly`: 시간별 예보
            - `.daily`: 일별 예보
            - `.minute`: 분별 강수 예보
            - `.alerts`: 기상 특보
        }
        
        @Steps {
            @Step {
                weather(for:including:) 메서드로 특정 DataSet만 요청합니다.
                
                @Code(name: "DataSet.swift", file: "02-04-dataset.swift")
            }
            
            @Step {
                여러 DataSet을 동시에 요청할 수 있습니다.
                튜플로 결과를 받아 각 데이터에 접근합니다.
                
                @Code(name: "DataSet.swift", file: "02-05-multiple.swift")
            }
            
            @Step {
                가변 개수의 DataSet을 요청하는 방법입니다.
                WeatherQuery를 사용해 동적으로 데이터를 요청합니다.
                
                @Code(name: "DataSet.swift", file: "02-06-query.swift")
            }
        }
    }
    
    @Section(title: "에러 처리") {
        @ContentAndMedia {
            WeatherKit 요청은 다양한 이유로 실패할 수 있습니다.
            적절한 에러 처리로 사용자 경험을 개선합니다.
        }
        
        @Steps {
            @Step {
                WeatherError 타입을 사용해 에러를 처리합니다.
                
                @Code(name: "ErrorHandling.swift", file: "02-07-error.swift")
            }
            
            @Step {
                네트워크 에러와 WeatherKit 특정 에러를 구분합니다.
                
                - `.permissionDenied`: 권한 거부
                - `.unknown`: 알 수 없는 에러
                - 네트워크 에러: URLError 등
                
                @Code(name: "ErrorHandling.swift", file: "02-08-error-types.swift")
            }
            
            @Step {
                사용자에게 친화적인 에러 메시지를 표시합니다.
                
                @Code(name: "ErrorHandling.swift", file: "02-09-user-message.swift")
            }
        }
    }
    
    @Assessments {
        @MultipleChoice {
            WeatherService에서 현재 날씨만 요청하려면 어떤 메서드를 사용해야 하나요?
            
            @Choice(isCorrect: false) {
                weather(for: location)
                
                @Justification(reaction: "부분적으로 맞습니다.") {
                    이 메서드도 사용 가능하지만, 모든 데이터를 요청하므로 
                    API 사용량이 증가합니다.
                }
            }
            
            @Choice(isCorrect: true) {
                weather(for: location, including: .current)
                
                @Justification(reaction: "정확합니다! 🎯") {
                    including 파라미터로 필요한 DataSet만 지정하면 
                    효율적으로 API를 사용할 수 있습니다.
                }
            }
            
            @Choice(isCorrect: false) {
                currentWeather(for: location)
                
                @Justification(reaction: "이런 메서드는 없습니다.") {
                    WeatherService는 weather(for:including:) 메서드를 사용합니다.
                }
            }
        }
        
        @MultipleChoice {
            WeatherKit API 요청이 실패했을 때 확인해야 할 사항이 아닌 것은?
            
            @Choice(isCorrect: false) {
                네트워크 연결 상태
                
                @Justification(reaction: "확인해야 합니다.") {
                    네트워크 연결은 API 요청의 기본 요구사항입니다.
                }
            }
            
            @Choice(isCorrect: false) {
                WeatherKit capability 설정 여부
                
                @Justification(reaction: "확인해야 합니다.") {
                    capability가 설정되지 않으면 권한 에러가 발생합니다.
                }
            }
            
            @Choice(isCorrect: true) {
                Apple Weather 앱 로그인 상태
                
                @Justification(reaction: "맞습니다! ✨") {
                    WeatherKit은 Apple Weather 앱과 독립적으로 동작합니다.
                    앱 로그인 상태는 API 요청에 영향을 주지 않습니다.
                }
            }
        }
    }
}
