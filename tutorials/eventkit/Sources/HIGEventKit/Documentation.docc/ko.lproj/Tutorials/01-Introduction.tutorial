@Tutorial(time: 10) {
    @Intro(title: "EventKit 소개 & 권한") {
        EventKit 프레임워크의 핵심 개념과 
        캘린더/리마인더 접근을 위한 권한 설정을 배웁니다.
    }
    
    @Section(title: "EventKit이란?") {
        @ContentAndMedia {
            EventKit은 iOS와 macOS에서 캘린더 이벤트와 
            리마인더에 접근할 수 있게 해주는 프레임워크입니다.
            
            사용자의 캘린더 앱 데이터를 읽고 쓸 수 있어
            일정 관리 기능을 앱에 통합할 수 있습니다.
        }
        
        @Steps {
            @Step {
                EventKit의 주요 클래스들을 살펴봅니다.
                
                @Code(name: "EventKitOverview.swift", file: "01-01-overview.swift")
            }
            
            @Step {
                프로젝트에 EventKit을 import합니다.
                
                @Code(name: "CalendarManager.swift", file: "01-02-import.swift")
            }
        }
    }
    
    @Section(title: "Info.plist 권한 설정") {
        @ContentAndMedia {
            EventKit을 사용하려면 Info.plist에 
            권한 요청 메시지를 추가해야 합니다.
            
            iOS 17부터는 캘린더와 리마인더 권한이 
            더 세분화되었습니다.
        }
        
        @Steps {
            @Step {
                Info.plist에 캘린더 접근 권한을 추가합니다.
                
                `NSCalendarsFullAccessUsageDescription` 키를 사용합니다.
                
                @Code(name: "Info.plist", file: "01-03-plist.swift")
            }
            
            @Step {
                리마인더 권한도 함께 추가합니다.
                
                `NSRemindersFullAccessUsageDescription` 키를 사용합니다.
                
                @Code(name: "Info.plist", file: "01-04-plist-reminder.swift")
            }
        }
    }
    
    @Section(title: "권한 요청 구현") {
        @ContentAndMedia {
            앱 실행 시 사용자에게 권한을 요청하는 
            코드를 작성합니다.
        }
        
        @Steps {
            @Step {
                `EKEventStore`를 생성하고 권한 상태를 확인합니다.
                
                @Code(name: "CalendarManager.swift", file: "01-05-auth-check.swift")
            }
            
            @Step {
                캘린더 접근 권한을 요청하는 메서드를 추가합니다.
                
                iOS 17 이상에서는 `requestFullAccessToEvents()`를 사용합니다.
                
                @Code(name: "CalendarManager.swift", file: "01-06-request-auth.swift")
            }
            
            @Step {
                권한 상태에 따른 처리 로직을 완성합니다.
                
                @Code(name: "CalendarManager.swift", file: "01-07-auth-complete.swift")
            }
        }
    }
    
    @Assessments {
        @MultipleChoice {
            EventKit에서 캘린더 데이터베이스에 접근하기 위해 사용하는 클래스는?
            
            @Choice(isCorrect: false) {
                `EKCalendar`
                
                @Justification(reaction: "다시 생각해보세요.") {
                    `EKCalendar`는 개별 캘린더를 나타내는 클래스입니다.
                }
            }
            
            @Choice(isCorrect: true) {
                `EKEventStore`
                
                @Justification(reaction: "정답입니다! 🎉") {
                    `EKEventStore`는 캘린더 데이터베이스에 대한 
                    연결을 관리하는 핵심 클래스입니다.
                }
            }
            
            @Choice(isCorrect: false) {
                `EKEvent`
                
                @Justification(reaction: "다시 생각해보세요.") {
                    `EKEvent`는 개별 이벤트를 나타내는 클래스입니다.
                }
            }
        }
        
        @MultipleChoice {
            iOS 17에서 캘린더 전체 접근 권한을 요청하는 메서드는?
            
            @Choice(isCorrect: false) {
                `requestAccess(to:)`
                
                @Justification(reaction: "다시 생각해보세요.") {
                    이 메서드는 iOS 17 이전 버전에서 사용됩니다.
                }
            }
            
            @Choice(isCorrect: true) {
                `requestFullAccessToEvents()`
                
                @Justification(reaction: "정답입니다! 🎉") {
                    iOS 17부터는 `requestFullAccessToEvents()`로 
                    캘린더 전체 접근 권한을 요청합니다.
                }
            }
            
            @Choice(isCorrect: false) {
                `requestCalendarAccess()`
                
                @Justification(reaction: "다시 생각해보세요.") {
                    이런 메서드는 존재하지 않습니다.
                }
            }
        }
    }
}
