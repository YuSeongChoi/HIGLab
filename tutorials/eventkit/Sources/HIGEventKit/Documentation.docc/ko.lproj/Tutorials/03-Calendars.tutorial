@Tutorial(time: 10) {
    @Intro(title: "캘린더 목록 가져오기") {
        사용자의 모든 캘린더를 가져와서 
        목록으로 표시하는 방법을 배웁니다.
    }
    
    @Section(title: "캘린더 조회하기") {
        @ContentAndMedia {
            `EKEventStore`의 `calendars(for:)` 메서드로 
            사용자의 캘린더 목록을 가져올 수 있습니다.
        }
        
        @Steps {
            @Step {
                이벤트용 캘린더 목록을 가져옵니다.
                
                @Code(name: "CalendarManager.swift", file: "03-01-fetch.swift")
            }
            
            @Step {
                각 캘린더의 속성들을 확인합니다.
                
                캘린더에는 제목, 색상, 소스 등의 정보가 있습니다.
                
                @Code(name: "CalendarManager.swift", file: "03-02-properties.swift")
            }
            
            @Step {
                기본 캘린더를 확인합니다.
                
                @Code(name: "CalendarManager.swift", file: "03-03-default.swift")
            }
        }
    }
    
    @Section(title: "캘린더 소스 이해하기") {
        @ContentAndMedia {
            캘린더는 여러 소스(iCloud, Google, Exchange 등)에서 
            올 수 있으며, 각 소스별로 그룹화할 수 있습니다.
        }
        
        @Steps {
            @Step {
                캘린더 소스 타입을 확인합니다.
                
                @Code(name: "CalendarManager.swift", file: "03-04-sources.swift")
            }
            
            @Step {
                소스별로 캘린더를 그룹화합니다.
                
                @Code(name: "CalendarManager.swift", file: "03-05-group.swift")
            }
        }
    }
    
    @Section(title: "캘린더 목록 UI 구현") {
        @ContentAndMedia {
            SwiftUI로 캘린더 목록을 표시하는 
            View를 만들어봅니다.
        }
        
        @Steps {
            @Step {
                캘린더 리스트 View를 만듭니다.
                
                @Code(name: "CalendarListView.swift", file: "03-06-list.swift")
            }
            
            @Step {
                각 캘린더의 색상을 표시합니다.
                
                @Code(name: "CalendarListView.swift", file: "03-07-color.swift")
            }
            
            @Step {
                캘린더 선택 기능을 추가합니다.
                
                @Code(name: "CalendarListView.swift", file: "03-08-select.swift")
            }
        }
    }
    
    @Assessments {
        @MultipleChoice {
            이벤트용 캘린더를 가져오는 올바른 코드는?
            
            @Choice(isCorrect: true) {
                `eventStore.calendars(for: .event)`
                
                @Justification(reaction: "정답입니다! 🎉") {
                    `.event` 타입을 지정하여 
                    이벤트용 캘린더만 가져옵니다.
                }
            }
            
            @Choice(isCorrect: false) {
                `eventStore.allCalendars()`
                
                @Justification(reaction: "다시 생각해보세요.") {
                    이런 메서드는 존재하지 않습니다.
                }
            }
            
            @Choice(isCorrect: false) {
                `eventStore.calendars()`
                
                @Justification(reaction: "다시 생각해보세요.") {
                    엔티티 타입을 지정해야 합니다.
                }
            }
        }
        
        @MultipleChoice {
            캘린더의 기본 색상을 가져오는 프로퍼티는?
            
            @Choice(isCorrect: false) {
                `calendar.color`
                
                @Justification(reaction: "다시 생각해보세요.") {
                    SwiftUI Color가 아닌 다른 타입입니다.
                }
            }
            
            @Choice(isCorrect: true) {
                `calendar.cgColor`
                
                @Justification(reaction: "정답입니다! 🎉") {
                    `cgColor` 프로퍼티로 캘린더의 색상을 
                    `CGColor` 타입으로 가져올 수 있습니다.
                }
            }
            
            @Choice(isCorrect: false) {
                `calendar.tintColor`
                
                @Justification(reaction: "다시 생각해보세요.") {
                    이런 프로퍼티는 존재하지 않습니다.
                }
            }
        }
    }
}
