@Tutorial(time: 20) {
    @Intro(title: "Siri 팁 & 제안") {
        사용자에게 앱의 Siri 기능을 알리고 적절한 시점에 제안합니다.
        
        @Image(source: "placeholder.png", alt: "Siri Tips")
    }
    
    @Section(title: "SiriTipView") {
        @ContentAndMedia {
            `SiriTipView`로 앱 내에서 Siri 사용법을 안내합니다.
            
            - 음성 명령 문구 표시
            - 탭하면 Siri 실행
            - 앱의 맥락에 맞는 시점에 노출
        }
        
        @Steps {
            @Step {
                SwiftUI 뷰에 `SiriTipView`를 추가합니다.
                
                표시할 Intent를 지정하면 해당 음성 명령을 보여줍니다.
                
                @Code(name: "ContentView.swift", file: "10-siritip-basic.swift")
            }
            
            @Step {
                적절한 시점에 팁을 표시합니다.
                
                사용자가 관련 작업을 할 때 자연스럽게 노출합니다.
                
                @Code(name: "ContentView.swift", file: "10-siritip-contextual.swift")
            }
        }
    }
    
    @Section(title: "ShortcutsLink") {
        @ContentAndMedia {
            `ShortcutsLink`로 단축어 앱으로 바로 이동하는 버튼을 제공합니다.
        }
        
        @Steps {
            @Step {
                앱 설정이나 온보딩에 ShortcutsLink를 추가합니다.
                
                @Code(name: "SettingsView.swift", file: "10-shortcutslink.swift")
            }
        }
    }
    
    @Section(title: "Spotlight 제안") {
        @ContentAndMedia {
            앱 사용 패턴을 기반으로 Spotlight에서 Intent를 제안합니다.
            
            - `donate()`로 사용 기록 저장
            - 시스템이 패턴을 학습
            - 적절한 시점에 Spotlight에 제안
        }
        
        @Steps {
            @Step {
                사용자 액션 후 Intent를 donate합니다.
                
                @Code(name: "BookActions.swift", file: "10-donate.swift")
            }
            
            @Step {
                RelevantContext로 제안 시점을 지정합니다.
                
                @Code(name: "BookActions.swift", file: "10-relevant.swift")
            }
        }
    }
    
    @Section(title: "인텐트 발견성 높이기") {
        @ContentAndMedia {
            사용자가 앱의 Siri 기능을 더 쉽게 발견하도록 합니다.
            
            - 온보딩에서 음성 명령 소개
            - 설정 화면에 Siri 섹션 추가
            - 맥락에 맞는 팁 표시
        }
        
        @Steps {
            @Step {
                종합 예제: Siri 기능 발견성을 높이는 패턴들을 적용합니다.
                
                @Code(name: "SiriDiscovery.swift", file: "10-discovery.swift")
            }
        }
    }
    
    @Assessments {
        @MultipleChoice {
            앱 내에서 "이렇게 말해보세요: ..." 형태의 Siri 팁을 표시하려면?
            
            @Choice(isCorrect: false) {
                `SiriSuggestionView`
                
                @Justification(reaction: "아니에요!") {
                    이런 뷰는 없습니다.
                }
            }
            
            @Choice(isCorrect: true) {
                `SiriTipView`
                
                @Justification(reaction: "정답! 🗣️") {
                    SiriTipView로 음성 명령 안내를 표시합니다.
                }
            }
            
            @Choice(isCorrect: false) {
                `IntentTipView`
                
                @Justification(reaction: "틀렸습니다!") {
                    이런 뷰는 App Intents에 없습니다.
                }
            }
        }
        
        @MultipleChoice {
            Spotlight에 Intent를 제안하려면 무엇을 호출해야 하나요?
            
            @Choice(isCorrect: true) {
                `donate()`
                
                @Justification(reaction: "맞습니다! ✨") {
                    Intent의 donate() 메서드로 시스템에 사용 기록을 전달합니다.
                }
            }
            
            @Choice(isCorrect: false) {
                `suggest()`
                
                @Justification(reaction: "아쉽네요!") {
                    이런 메서드는 없습니다.
                }
            }
            
            @Choice(isCorrect: false) {
                `register()`
                
                @Justification(reaction: "다시 생각해보세요!") {
                    register는 제안과 관련된 메서드가 아닙니다.
                }
            }
        }
        
        @MultipleChoice {
            앱에서 단축어 앱으로 바로 이동하는 버튼을 만들려면?
            
            @Choice(isCorrect: false) {
                `Link(destination: .shortcuts)`
                
                @Justification(reaction: "아니에요!") {
                    SwiftUI Link는 URL 기반이고 이 문법은 없습니다.
                }
            }
            
            @Choice(isCorrect: true) {
                `ShortcutsLink`
                
                @Justification(reaction: "정답! 🔗") {
                    ShortcutsLink는 단축어 앱으로 이동하는 전용 뷰입니다.
                }
            }
            
            @Choice(isCorrect: false) {
                `Button { openShortcuts() }`
                
                @Justification(reaction: "틀렸습니다!") {
                    openShortcuts() 같은 함수는 없습니다. ShortcutsLink를 사용하세요.
                }
            }
        }
    }
}
