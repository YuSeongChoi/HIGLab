@Tutorial(time: 20) {
    @Intro(title: "파티클 시스템") {
        SKEmitterNode를 사용하여 폭발, 불꽃, 연기 등 
        화려한 파티클 효과를 구현합니다.
        
        @Image(source: "chapter8-particle.png", alt: "파티클 시스템")
    }
    
    @Section(title: "파티클 이미터 생성") {
        @ContentAndMedia {
            `SKEmitterNode`는 파티클을 방출하는 노드입니다.
            Xcode의 파티클 에디터로 시각적으로 편집하거나
            코드로 직접 생성할 수 있습니다.
            
            **파티클 사용 사례:**
            - 폭발 효과
            - 불, 연기
            - 마법 효과
            - 비, 눈
            - 궤적 효과
            
            @Image(source: "emitter-node.png", alt: "이미터 노드")
        }
        
        @Steps {
            @Step {
                Xcode에서 파티클 파일을 생성합니다.
                File > New > File에서 SpriteKit Particle File을 선택합니다.
                
                @Image(source: "create-particle-file.png", alt: "파티클 파일 생성")
            }
            
            @Step {
                .sks 파일에서 이미터를 로드합니다.
                `SKEmitterNode(fileNamed:)`로 파일을 불러옵니다.
                
                @Code(name: "GameScene.swift", file: "08-particle-01.swift")
            }
            
            @Step {
                코드로 이미터를 생성합니다.
                각 속성을 직접 설정하여 파티클을 만듭니다.
                
                @Code(name: "GameScene.swift", file: "08-particle-02.swift")
            }
        }
    }
    
    @Section(title: "파티클 속성 조절") {
        @ContentAndMedia {
            파티클의 다양한 속성을 조절하여
            원하는 효과를 만들 수 있습니다.
            
            **주요 속성:**
            - `particleBirthRate`: 초당 생성 개수
            - `particleLifetime`: 파티클 수명
            - `particleSpeed`: 이동 속도
            - `emissionAngle`: 방출 각도
            - `particleScale`: 파티클 크기
            
            @Image(source: "particle-properties.png", alt: "파티클 속성")
        }
        
        @Steps {
            @Step {
                파티클 생성률과 수명을 설정합니다.
                더 많은 파티클, 더 오래 지속되는 효과를 만듭니다.
                
                @Code(name: "GameScene.swift", file: "08-particle-03.swift")
            }
            
            @Step {
                속도와 방향을 조절합니다.
                emissionAngle과 emissionAngleRange로 
                방출 방향을 제어합니다.
                
                @Code(name: "GameScene.swift", file: "08-particle-04.swift")
            }
            
            @Step {
                크기와 색상 변화를 설정합니다.
                시간에 따라 파티클이 커지거나 
                색이 변하도록 합니다.
                
                @Code(name: "GameScene.swift", file: "08-particle-05.swift")
            }
        }
    }
    
    @Section(title: "다양한 파티클 효과") {
        @ContentAndMedia {
            불, 연기, 폭발, 마법 등 
            다양한 효과를 파티클로 구현합니다.
            
            효과에 따라 적절한 속성 조합이 필요합니다.
            
            @Image(source: "particle-effects.png", alt: "다양한 파티클 효과")
        }
        
        @Steps {
            @Step {
                불꽃 효과를 만듭니다.
                위로 올라가면서 색이 변하는 파티클입니다.
                
                @Code(name: "GameScene.swift", file: "08-particle-06.swift")
            }
            
            @Step {
                폭발 효과를 만듭니다.
                순간적으로 사방으로 퍼지는 파티클입니다.
                
                @Code(name: "GameScene.swift", file: "08-particle-07.swift")
            }
            
            @Step {
                궤적 효과를 만듭니다.
                이동하는 객체 뒤에 남는 트레일입니다.
                
                @Code(name: "GameScene.swift", file: "08-particle-08.swift")
            }
        }
    }
    
    @Section(title: "파티클 최적화") {
        @ContentAndMedia {
            파티클은 성능에 영향을 줄 수 있습니다.
            적절한 최적화로 부드러운 게임플레이를 유지합니다.
            
            **최적화 팁:**
            - 화면 밖 파티클 제거
            - 최대 파티클 수 제한
            - 일시 정지 시 비활성화
            
            @Image(source: "particle-optimization.png", alt: "파티클 최적화")
        }
        
        @Steps {
            @Step {
                numParticlesToEmit으로 파티클 수를 제한합니다.
                무한 방출 대신 정해진 수만 생성합니다.
                
                @Code(name: "GameScene.swift", file: "08-particle-09.swift")
            }
            
            @Step {
                파티클 완료 후 자동 제거합니다.
                모든 파티클이 사라지면 노드를 제거합니다.
                
                @Code(name: "GameScene.swift", file: "08-particle-10.swift")
            }
            
            @Step {
                targetNode를 활용합니다.
                이미터가 이동해도 파티클은 제자리에 남습니다.
                
                @Code(name: "GameScene.swift", file: "08-particle-11.swift")
            }
        }
    }
    
    @Assessments {
        @MultipleChoice {
            파티클 파일에서 이미터를 로드하는 메서드는?
            
            @Choice(isCorrect: true) {
                `SKEmitterNode(fileNamed:)`
                
                @Justification(reaction: "정확합니다! ✨") {
                    `SKEmitterNode(fileNamed:)`로 
                    .sks 파티클 파일을 로드합니다.
                }
            }
            
            @Choice(isCorrect: false) {
                `SKEmitterNode(named:)`
                
                @Justification(reaction: "틀렸습니다.") {
                    정확한 메서드 이름은 
                    `SKEmitterNode(fileNamed:)`입니다.
                }
            }
            
            @Choice(isCorrect: false) {
                `SKEmitterNode.load(file:)`
                
                @Justification(reaction: "다시 확인하세요.") {
                    이런 메서드는 존재하지 않습니다.
                }
            }
        }
        
        @MultipleChoice {
            파티클의 초당 생성 개수를 조절하는 속성은?
            
            @Choice(isCorrect: false) {
                `particleSpawnRate`
                
                @Justification(reaction: "틀렸습니다.") {
                    이 속성은 존재하지 않습니다.
                }
            }
            
            @Choice(isCorrect: true) {
                `particleBirthRate`
                
                @Justification(reaction: "맞습니다! 🎯") {
                    `particleBirthRate`로 초당 
                    생성되는 파티클 수를 설정합니다.
                }
            }
            
            @Choice(isCorrect: false) {
                `emissionRate`
                
                @Justification(reaction: "다시 확인하세요.") {
                    SpriteKit에서는 `particleBirthRate`를 
                    사용합니다.
                }
            }
        }
        
        @MultipleChoice {
            한정된 수의 파티클만 방출하고 멈추게 하려면?
            
            @Choice(isCorrect: true) {
                `numParticlesToEmit` 속성 설정
                
                @Justification(reaction: "정확합니다! 💥") {
                    `numParticlesToEmit`을 양수로 설정하면
                    해당 개수만큼만 파티클을 방출하고 멈춥니다.
                    0이면 무한히 방출합니다.
                }
            }
            
            @Choice(isCorrect: false) {
                `particleCount` 속성 설정
                
                @Justification(reaction: "틀렸습니다.") {
                    이 속성은 현재 파티클 수를 나타내며,
                    설정용이 아닙니다.
                }
            }
            
            @Choice(isCorrect: false) {
                타이머로 이미터 제거
                
                @Justification(reaction: "가능하지만 권장하지 않습니다.") {
                    `numParticlesToEmit`을 사용하는 것이 
                    더 효율적이고 정확합니다.
                }
            }
        }
    }
}
