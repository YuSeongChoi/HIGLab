@Tutorial(time: 20) {
    @Intro(title: "NWBrowser (서비스 발견)") {
        Bonjour를 활용해 로컬 네트워크에서 
        서비스를 자동으로 발견합니다.
        
        @Image(source: "nwbrowser.png", alt: "NWBrowser 서비스 발견")
    }
    
    @Section(title: "Bonjour 서비스 발견") {
        @ContentAndMedia {
            NWBrowser는 로컬 네트워크에서 
            Bonjour 서비스를 탐색합니다.
            
            - **자동 발견**: IP 주소 없이 서비스 찾기
            - **실시간 업데이트**: 서비스 추가/제거 감지
            - **TXT 레코드**: 서비스 메타데이터 제공
            
            @Image(source: "bonjour-flow.png", alt: "Bonjour 동작 흐름")
        }
        
        @Steps {
            @Step {
                탐색할 서비스 타입을 정의합니다.
                서버와 동일한 타입을 사용해야 합니다.
                
                @Code(name: "BrowserDescriptor.swift", file: "06-01-browser-descriptor.swift")
            }
            
            @Step {
                NWBrowser를 생성합니다.
                파라미터로 프로토콜 옵션을 지정합니다.
                
                @Code(name: "BrowserCreate.swift", file: "06-02-browser-create.swift")
            }
        }
    }
    
    @Section(title: "서비스 목록 관리") {
        @ContentAndMedia {
            `browseResultsChangedHandler`로 
            발견된 서비스의 변화를 감지합니다.
            
            서비스가 추가되거나 제거될 때마다 호출됩니다.
        }
        
        @Steps {
            @Step {
                결과 변경 핸들러를 등록합니다.
                발견된 서비스 집합이 변경될 때 호출됩니다.
                
                @Code(name: "BrowseResults.swift", file: "06-03-browse-results.swift")
            }
            
            @Step {
                개별 변경 사항을 처리합니다.
                added와 removed를 구분해서 UI를 업데이트합니다.
                
                @Code(name: "ResultChanges.swift", file: "06-04-result-changes.swift")
            }
            
            @Step {
                브라우저를 시작하고 상태를 모니터링합니다.
                
                @Code(name: "BrowserStart.swift", file: "06-05-browser-start.swift")
            }
        }
    }
    
    @Section(title: "서비스 정보 추출") {
        @ContentAndMedia {
            NWBrowser.Result에서 서비스의 
            상세 정보를 추출합니다.
            
            엔드포인트, 인터페이스, 메타데이터 등을 
            확인할 수 있습니다.
        }
        
        @Steps {
            @Step {
                endpoint에서 서비스 이름을 추출합니다.
                사용자에게 표시할 친숙한 이름입니다.
                
                @Code(name: "ServiceName.swift", file: "06-06-service-name.swift")
            }
            
            @Step {
                TXT 레코드에서 메타데이터를 읽습니다.
                서버가 제공한 추가 정보를 확인합니다.
                
                @Code(name: "TXTMetadata.swift", file: "06-07-txt-metadata.swift")
            }
            
            @Step {
                인터페이스 정보를 확인합니다.
                Wi-Fi인지 유선인지 등을 알 수 있습니다.
                
                @Code(name: "InterfaceInfo.swift", file: "06-08-interface-info.swift")
            }
        }
    }
    
    @Section(title: "발견한 서비스에 연결") {
        @ContentAndMedia {
            NWBrowser.Result의 endpoint로 
            직접 NWConnection을 생성합니다.
            
            IP 주소를 알 필요 없이 서비스에 연결할 수 있습니다.
        }
        
        @Steps {
            @Step {
                발견된 서비스에 연결합니다.
                endpoint를 그대로 사용합니다.
                
                @Code(name: "ConnectToService.swift", file: "06-09-connect-service.swift")
            }
            
            @Step {
                P2P 피어 목록을 SwiftUI로 표시합니다.
                발견된 서비스를 탭하면 연결합니다.
                
                @Code(name: "PeerListView.swift", file: "06-10-peer-list.swift")
            }
            
            @Step {
                ViewModel로 브라우저 상태를 관리합니다.
                Combine을 활용해 UI를 업데이트합니다.
                
                @Code(name: "BrowserViewModel.swift", file: "06-11-browser-viewmodel.swift")
            }
        }
    }
    
    @Assessments {
        @MultipleChoice {
            NWBrowser.Result에서 얻을 수 있는 정보가 아닌 것은?
            
            @Choice(isCorrect: false) {
                서비스 endpoint
                
                @Justification(reaction: "다시 생각해보세요.") {
                    endpoint는 Result에서 제공됩니다.
                    이를 통해 직접 연결할 수 있습니다.
                }
            }
            
            @Choice(isCorrect: false) {
                TXT 레코드 메타데이터
                
                @Justification(reaction: "아쉽습니다.") {
                    metadata로 TXT 레코드에 접근할 수 있습니다.
                }
            }
            
            @Choice(isCorrect: true) {
                서비스의 CPU 사용량
                
                @Justification(reaction: "맞습니다! ✨") {
                    CPU 사용량 같은 시스템 정보는 
                    Bonjour로 제공되지 않습니다.
                }
            }
        }
        
        @MultipleChoice {
            Bonjour 서비스 타입의 올바른 형식은?
            
            @Choice(isCorrect: true) {
                `_mychat._tcp`
                
                @Justification(reaction: "정확합니다! 🎯") {
                    서비스 타입은 언더스코어로 시작하고,
                    프로토콜(_tcp 또는 _udp)을 명시합니다.
                }
            }
            
            @Choice(isCorrect: false) {
                `mychat.tcp`
                
                @Justification(reaction: "틀렸습니다.") {
                    언더스코어가 필요합니다.
                    올바른 형식은 `_mychat._tcp`입니다.
                }
            }
            
            @Choice(isCorrect: false) {
                `_mychat`
                
                @Justification(reaction: "다시 생각해보세요.") {
                    프로토콜 지정이 필요합니다.
                    `_tcp` 또는 `_udp`를 붙여야 합니다.
                }
            }
        }
    }
}
