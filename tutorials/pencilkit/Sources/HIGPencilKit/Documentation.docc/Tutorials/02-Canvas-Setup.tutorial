@Tutorial(time: 20) {
    @Intro(title: "PKCanvasView 설정") {
        PKCanvasView를 설정하고 SwiftUI 및 UIKit에서 
        드로잉 캔버스를 통합하는 방법을 배웁니다.
        
        @Image(source: "canvas-setup.png", alt: "PKCanvasView 설정")
    }
    
    @Section(title: "UIKit에서 PKCanvasView 설정") {
        @ContentAndMedia {
            PKCanvasView는 UIView의 서브클래스로,
            UIKit 앱에서 쉽게 통합할 수 있습니다.
            
            - **스크롤 지원**: UIScrollView 기반으로 확대/축소 가능
            - **델리게이트**: PKCanvasViewDelegate로 드로잉 변화 감지
            - **드로잉 정책**: 손가락/Apple Pencil 입력 제어
            
            @Image(source: "canvas-uikit.png", alt: "UIKit 캔버스")
        }
        
        @Steps {
            @Step {
                UIViewController에 PKCanvasView를 추가합니다.
                기본적인 레이아웃 제약을 설정합니다.
                
                @Code(name: "BasicCanvasViewController.swift", file: "02-01-basic-canvas.swift")
            }
            
            @Step {
                PKCanvasViewDelegate를 구현하여 
                드로잉 변경을 감지합니다.
                
                @Code(name: "CanvasDelegate.swift", file: "02-02-delegate.swift")
            }
            
            @Step {
                캔버스의 배경색과 불투명도를 설정합니다.
                투명 배경을 사용하면 아래 레이어가 보입니다.
                
                @Code(name: "CanvasBackground.swift", file: "02-03-background.swift")
            }
        }
    }
    
    @Section(title: "SwiftUI에서 PKCanvasView 사용하기") {
        @ContentAndMedia {
            SwiftUI에서는 UIViewRepresentable을 사용하여
            PKCanvasView를 래핑합니다.
            
            @Binding을 통해 PKDrawing 데이터를 
            양방향으로 동기화할 수 있습니다.
        }
        
        @Steps {
            @Step {
                UIViewRepresentable을 구현하여 
                PKCanvasView를 SwiftUI에서 사용할 수 있게 만듭니다.
                
                @Code(name: "CanvasView.swift", file: "02-04-swiftui-canvas.swift")
            }
            
            @Step {
                Coordinator를 사용하여 
                PKCanvasViewDelegate 이벤트를 처리합니다.
                
                @Code(name: "CanvasCoordinator.swift", file: "02-05-coordinator.swift")
            }
            
            @Step {
                SwiftUI View에서 CanvasView를 사용합니다.
                @State로 PKDrawing을 관리합니다.
                
                @Code(name: "DrawingView.swift", file: "02-06-drawing-view.swift")
            }
        }
    }
    
    @Section(title: "캔버스 확대/축소 설정") {
        @ContentAndMedia {
            PKCanvasView는 UIScrollView를 상속받아
            핀치 제스처로 확대/축소가 가능합니다.
            
            - minimumZoomScale: 최소 축소 비율
            - maximumZoomScale: 최대 확대 비율
            - zoomScale: 현재 확대 비율
        }
        
        @Steps {
            @Step {
                확대/축소 범위를 설정하고 
                더블 탭으로 줌하는 기능을 추가합니다.
                
                @Code(name: "ZoomableCanvas.swift", file: "02-07-zoomable.swift")
            }
            
            @Step {
                확대 상태에서 드로잉 품질을 유지하려면
                contentScaleFactor를 적절히 설정합니다.
                
                @Code(name: "HighResCanvas.swift", file: "02-08-high-res.swift")
            }
        }
    }
    
    @Assessments {
        @MultipleChoice {
            SwiftUI에서 PKCanvasView를 사용하려면 어떤 프로토콜을 구현해야 하나요?
            
            @Choice(isCorrect: true) {
                UIViewRepresentable
                
                @Justification(reaction: "정확합니다! 🎯") {
                    UIViewRepresentable을 구현하면 UIKit 뷰를 
                    SwiftUI에서 사용할 수 있습니다.
                }
            }
            
            @Choice(isCorrect: false) {
                ViewModifier
                
                @Justification(reaction: "아쉽습니다.") {
                    ViewModifier는 뷰를 수정하는 데 사용됩니다.
                    UIKit 뷰 래핑에는 UIViewRepresentable을 사용하세요.
                }
            }
            
            @Choice(isCorrect: false) {
                ObservableObject
                
                @Justification(reaction: "다시 생각해보세요.") {
                    ObservableObject는 데이터 모델에 사용됩니다.
                    뷰 래핑과는 다른 목적입니다.
                }
            }
        }
        
        @MultipleChoice {
            PKCanvasViewDelegate의 canvasViewDrawingDidChange 메서드는 언제 호출되나요?
            
            @Choice(isCorrect: false) {
                캔버스가 화면에 나타날 때
                
                @Justification(reaction: "틀렸습니다.") {
                    이 메서드는 드로잉 내용이 변경될 때만 호출됩니다.
                }
            }
            
            @Choice(isCorrect: true) {
                사용자가 드로잉을 완료하거나 수정할 때
                
                @Justification(reaction: "맞습니다! ✨") {
                    canvasViewDrawingDidChange는 PKDrawing이 
                    변경될 때마다 호출됩니다.
                }
            }
            
            @Choice(isCorrect: false) {
                매 프레임마다
                
                @Justification(reaction: "아닙니다.") {
                    성능을 위해 드로잉 완료 시점에만 호출됩니다.
                }
            }
        }
    }
}
