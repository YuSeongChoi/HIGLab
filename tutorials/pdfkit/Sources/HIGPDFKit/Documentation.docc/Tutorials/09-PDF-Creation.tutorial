@Tutorial(time: 25) {
    @Intro(title: "PDF 생성") {
        프로그래밍 방식으로 PDF 문서를 생성합니다.
        텍스트, 이미지, 도형 등을 포함한 문서를 만듭니다.
        
        @Image(source: "pdf-creation.png", alt: "PDF 생성")
    }
    
    @Section(title: "UIGraphicsPDFRenderer 기초") {
        @ContentAndMedia {
            iOS에서 PDF를 생성하는 가장 현대적인 방법은 
            UIGraphicsPDFRenderer를 사용하는 것입니다.
            
            Core Graphics 컨텍스트에 직접 그리면서
            여러 페이지의 PDF를 만들 수 있습니다.
        }
        
        @Steps {
            @Step {
                UIGraphicsPDFRenderer를 생성하고 
                기본 PDF를 만듭니다.
                
                @Code(name: "BasicPDFRenderer.swift", file: "09-01-renderer.swift")
            }
            
            @Step {
                페이지 크기와 마진을 설정합니다.
                A4, Letter 등 표준 크기를 사용합니다.
                
                @Code(name: "PageSettings.swift", file: "09-02-page-settings.swift")
            }
            
            @Step {
                PDF 메타데이터를 설정합니다.
                제목, 작성자, 주제 등의 정보를 추가합니다.
                
                @Code(name: "PDFMetadata.swift", file: "09-03-metadata.swift")
            }
        }
    }
    
    @Section(title: "텍스트 추가하기") {
        @ContentAndMedia {
            PDF에 텍스트를 추가하는 다양한 방법을 배웁니다.
            
            NSAttributedString으로 서식 있는 텍스트를,
            draw(at:) 또는 draw(in:)으로 렌더링합니다.
        }
        
        @Steps {
            @Step {
                기본 텍스트를 PDF에 추가합니다.
                String.draw(at:withAttributes:) 메서드를 사용합니다.
                
                @Code(name: "AddText.swift", file: "09-04-add-text.swift")
            }
            
            @Step {
                NSAttributedString으로 서식 있는 텍스트를 추가합니다.
                폰트, 색상, 정렬 등을 지정합니다.
                
                @Code(name: "AttributedText.swift", file: "09-05-attributed.swift")
            }
            
            @Step {
                텍스트가 영역에 맞지 않을 때 페이지를 나눕니다.
                NSLayoutManager 또는 boundingRect를 활용합니다.
                
                @Code(name: "TextPagination.swift", file: "09-06-pagination.swift")
            }
        }
    }
    
    @Section(title: "이미지 추가하기") {
        @ContentAndMedia {
            PDF에 UIImage를 추가합니다.
            크기 조정, 위치 지정, 종횡비 유지 등을 처리합니다.
        }
        
        @Steps {
            @Step {
                UIImage를 PDF에 그립니다.
                draw(in:) 메서드로 영역을 지정합니다.
                
                @Code(name: "AddImage.swift", file: "09-07-add-image.swift")
            }
            
            @Step {
                종횡비를 유지하면서 이미지를 추가합니다.
                AVFoundation의 AVMakeRect를 활용합니다.
                
                @Code(name: "AspectRatioImage.swift", file: "09-08-aspect-ratio.swift")
            }
            
            @Step {
                이미지 품질과 압축을 조정합니다.
                JPEG 품질을 낮춰 파일 크기를 줄입니다.
                
                @Code(name: "ImageCompression.swift", file: "09-09-compression.swift")
            }
        }
    }
    
    @Section(title: "도형과 그래픽") {
        @ContentAndMedia {
            Core Graphics로 선, 사각형, 원, 경로 등을 그립니다.
            차트나 다이어그램에 활용합니다.
        }
        
        @Steps {
            @Step {
                기본 도형을 그립니다.
                CGContext의 fill, stroke 메서드를 사용합니다.
                
                @Code(name: "DrawShapes.swift", file: "09-10-shapes.swift")
            }
            
            @Step {
                UIBezierPath로 복잡한 경로를 그립니다.
                곡선과 사용자 정의 도형을 만듭니다.
                
                @Code(name: "BezierPaths.swift", file: "09-11-bezier.swift")
            }
            
            @Step {
                테이블 레이아웃을 그립니다.
                격자선과 셀 내 텍스트를 배치합니다.
                
                @Code(name: "DrawTable.swift", file: "09-12-table.swift")
            }
        }
    }
    
    @Section(title: "PDF 저장 및 공유") {
        @ContentAndMedia {
            생성된 PDF를 파일로 저장하거나
            다른 앱과 공유합니다.
        }
        
        @Steps {
            @Step {
                PDF 데이터를 파일로 저장합니다.
                Documents 디렉토리에 저장합니다.
                
                @Code(name: "SavePDF.swift", file: "09-13-save.swift")
            }
            
            @Step {
                UIActivityViewController로 PDF를 공유합니다.
                이메일, AirDrop, Files 앱 등으로 내보냅니다.
                
                @Code(name: "SharePDF.swift", file: "09-14-share.swift")
            }
            
            @Step {
                생성된 PDF를 PDFDocument로 로드하여 확인합니다.
                Data에서 직접 PDFDocument를 생성합니다.
                
                @Code(name: "VerifyPDF.swift", file: "09-15-verify.swift")
            }
        }
    }
    
    @Assessments {
        @MultipleChoice {
            iOS에서 PDF를 생성하는 현대적인 API는?
            
            @Choice(isCorrect: true) {
                UIGraphicsPDFRenderer
                
                @Justification(reaction: "정확합니다! 🎯") {
                    UIGraphicsPDFRenderer는 iOS 10에서 도입된 
                    현대적인 PDF 생성 API입니다.
                }
            }
            
            @Choice(isCorrect: false) {
                PDFGenerator
                
                @Justification(reaction: "그런 클래스는 없습니다.") {
                    Apple의 PDF 생성 API를 확인해보세요.
                }
            }
            
            @Choice(isCorrect: false) {
                PDFDocument.create()
                
                @Justification(reaction: "아쉽습니다.") {
                    PDFDocument는 기존 PDF를 로드하는 데 사용되며,
                    새 문서 생성은 다른 API를 사용합니다.
                }
            }
        }
        
        @MultipleChoice {
            UIGraphicsPDFRenderer에서 새 페이지를 시작하는 메서드는?
            
            @Choice(isCorrect: false) {
                newPage()
                
                @Justification(reaction: "그런 메서드는 없습니다.") {
                    PDF 렌더러 컨텍스트의 메서드를 확인해보세요.
                }
            }
            
            @Choice(isCorrect: true) {
                beginPage()
                
                @Justification(reaction: "맞습니다! ✨") {
                    context.beginPage()를 호출하여 
                    새 페이지를 시작합니다.
                }
            }
            
            @Choice(isCorrect: false) {
                addPage()
                
                @Justification(reaction: "틀렸습니다.") {
                    정확한 메서드명을 확인해보세요.
                }
            }
        }
        
        @MultipleChoice {
            PDF 메타데이터(제목, 작성자 등)를 설정하는 방법은?
            
            @Choice(isCorrect: true) {
                UIGraphicsPDFRendererFormat의 documentInfo
                
                @Justification(reaction: "정확합니다! 🎯") {
                    format.documentInfo 딕셔너리에 
                    kCGPDFContextTitle 등의 키로 메타데이터를 설정합니다.
                }
            }
            
            @Choice(isCorrect: false) {
                PDFDocument.documentAttributes
                
                @Justification(reaction: "아쉽습니다.") {
                    이것은 기존 PDF의 속성을 읽는 데 사용되며,
                    생성 시에는 다른 방법을 사용합니다.
                }
            }
            
            @Choice(isCorrect: false) {
                CGContext.setMetadata()
                
                @Justification(reaction: "그런 메서드는 없습니다.") {
                    PDF 렌더러 포맷의 속성을 확인해보세요.
                }
            }
        }
    }
}
