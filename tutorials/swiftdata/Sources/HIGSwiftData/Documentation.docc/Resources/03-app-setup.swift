import SwiftUI
import SwiftData

// TaskMaster 앱 - SwiftData 연결 완료!

@main
struct TaskMasterApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        // 🎉 SwiftData 설정 완료!
        .modelContainer(for: TaskItem.self)
    }
}

// ─────────────────────────────────────────

// 이제 모든 하위 뷰에서:
// 1. @Environment(\.modelContext) 사용 가능
// 2. @Query로 데이터 조회 가능
// 3. CRUD 연산 가능

// 다음 단계:
// - Chapter 4: @Query로 데이터 읽기
// - Chapter 5: CRUD 구현
// - Chapter 6: Category 모델 추가

// ─────────────────────────────────────────

// 앱 생명주기에서 SwiftData 동작:
// - 앱 시작: Container & Context 자동 초기화
// - 앱 실행 중: autosave로 자동 저장
// - 앱 백그라운드: pending changes 자동 저장
// - 앱 종료: 최종 저장 시도

// 데이터 손실 걱정 없이 개발에 집중하세요! 🚀
