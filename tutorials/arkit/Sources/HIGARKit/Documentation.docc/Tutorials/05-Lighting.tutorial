@Tutorial(time: 20) {
    @Intro(title: "조명과 그림자") {
        AR 환경의 조명 추정과 그림자 효과를 구현합니다.
    }
    
    @Section(title: "환경 조명 추정") {
        @ContentAndMedia {
            ARKit의 조명 추정 기능으로 가상 객체가 실제 환경과 자연스럽게 어우러지도록 합니다.
        }
        @Steps {
            @Step {
                ARWorldTrackingConfiguration에서 조명 추정을 활성화합니다.
                @Code(name: "LightingSetup.swift", file: "05-lighting.swift")
            }
            @Step {
                DirectionalLight와 PointLight로 조명을 설정합니다.
                @Code(name: "LightingSetup.swift", file: "05-lighting-02.swift")
            }
            @Step {
                그림자를 받을 평면을 구성합니다.
                @Code(name: "LightingSetup.swift", file: "05-lighting-03.swift")
            }
        }
    }
    
    @Assessments {
        @MultipleChoice {
            ARKit에서 실제 환경의 조명 강도를 추정하는 데 사용되는 속성은?
            
            @Choice(isCorrect: false) {
                `ambientColorTemperature`
                @Justification(reaction: "다시 생각해보세요.") {
                    색온도는 조명의 색상을 나타내며, 강도와는 다릅니다.
                }
            }
            @Choice(isCorrect: true) {
                `ambientIntensity`
                @Justification(reaction: "정답입니다! 🎉") {
                    `ambientIntensity`는 환경의 조명 강도를 루멘 단위로 제공합니다.
                }
            }
            @Choice(isCorrect: false) {
                `lightEstimation`
                @Justification(reaction: "다시 생각해보세요.") {
                    이것은 조명 추정을 활성화하는 설정이지, 속성이 아닙니다.
                }
            }
        }
    }
}
