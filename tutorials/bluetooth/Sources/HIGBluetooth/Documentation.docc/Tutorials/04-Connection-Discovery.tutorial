@Tutorial(time: 20) {
    @Intro(title: "연결 & 서비스 발견") {
        선택한 BLE 기기에 연결하고
        제공하는 서비스와 특성을 탐색합니다.
    }
    
    @Section(title: "기기 연결") {
        @ContentAndMedia {
            `connect` 메서드로 Peripheral에 연결합니다.
            연결 과정은 비동기적이며 delegate 콜백으로 결과를 받습니다.
            
            **연결 옵션:**
            - `notifyOnConnection`: 연결 시 알림
            - `notifyOnDisconnection`: 해제 시 알림
            - `enableAutoReconnect`: iOS 17+ 자동 재연결
        }
        
        @Steps {
            @Step {
                선택한 기기에 연결하는 기능을 구현합니다.
                Peripheral 참조를 유지해야 합니다.
                
                @Code(name: "BluetoothManager.swift", file: "04-01-connect.swift")
            }
            
            @Step {
                연결 성공/실패 콜백을 처리합니다.
                연결 타임아웃도 구현합니다.
                
                @Code(name: "BluetoothManager.swift", file: "04-02-connection-callbacks.swift")
            }
        }
    }
    
    @Section(title: "서비스 발견") {
        @ContentAndMedia {
            연결 후 `discoverServices`를 호출하여
            기기가 제공하는 서비스 목록을 가져옵니다.
            
            특정 서비스 UUID를 지정하거나
            nil로 모든 서비스를 발견할 수 있습니다.
        }
        
        @Steps {
            @Step {
                연결 성공 후 서비스 발견을 시작합니다.
                관심있는 서비스만 필터링합니다.
                
                @Code(name: "BluetoothManager.swift", file: "04-03-discover-services.swift")
            }
            
            @Step {
                발견된 서비스를 처리하는 콜백을 구현합니다.
                
                @Code(name: "BluetoothManager.swift", file: "04-04-services-callback.swift")
            }
        }
    }
    
    @Section(title: "Characteristic 발견") {
        @ContentAndMedia {
            각 서비스에서 `discoverCharacteristics`를 호출하여
            해당 서비스의 Characteristic을 발견합니다.
            
            Characteristic의 properties를 확인하여
            읽기/쓰기/알림 가능 여부를 파악합니다.
        }
        
        @Steps {
            @Step {
                서비스의 Characteristic을 발견합니다.
                
                @Code(name: "BluetoothManager.swift", file: "04-05-discover-characteristics.swift")
            }
            
            @Step {
                Characteristic의 속성(properties)을 분석합니다.
                어떤 작업이 가능한지 확인합니다.
                
                @Code(name: "CharacteristicProperties.swift", file: "04-06-properties.swift")
            }
            
            @Step {
                발견된 서비스와 Characteristic을 
                구조화된 모델로 저장합니다.
                
                @Code(name: "ServiceModel.swift", file: "04-07-service-model.swift")
            }
        }
    }
    
    @Assessments {
        @MultipleChoice {
            connect() 호출 후 Peripheral 연결이 성공하면 호출되는 콜백은?
            
            @Choice(isCorrect: true) {
                centralManager(_:didConnect:)
                
                @Justification(reaction: "정확합니다! 🔗") {
                    didConnect 콜백에서 연결 성공을 알 수 있습니다.
                    이 시점에 서비스 발견을 시작합니다.
                }
            }
            
            @Choice(isCorrect: false) {
                centralManager(_:didDiscover:)
                
                @Justification(reaction: "이건 스캔 콜백입니다.") {
                    didDiscover는 기기 발견 시 호출됩니다.
                }
            }
            
            @Choice(isCorrect: false) {
                peripheral(_:didDiscoverServices:)
                
                @Justification(reaction: "이건 서비스 발견 콜백입니다.") {
                    서비스 발견은 연결 후 별도로 요청해야 합니다.
                }
            }
        }
        
        @MultipleChoice {
            Characteristic의 properties에서 .notify가 있으면?
            
            @Choice(isCorrect: false) {
                값을 쓸 수 있다
                
                @Justification(reaction: "아닙니다.") {
                    쓰기는 .write 또는 .writeWithoutResponse입니다.
                }
            }
            
            @Choice(isCorrect: true) {
                값 변경 알림을 구독할 수 있다
                
                @Justification(reaction: "맞습니다! 🔔") {
                    .notify가 있으면 setNotifyValue(true)로
                    값 변경 시 알림을 받을 수 있습니다.
                }
            }
            
            @Choice(isCorrect: false) {
                값을 읽을 수 있다
                
                @Justification(reaction: "읽기는 .read입니다.") {
                    읽기 가능 여부는 .read 속성으로 확인합니다.
                }
            }
        }
        
        @MultipleChoice {
            discoverServices(nil)과 discoverServices([specificUUID])의 차이는?
            
            @Choice(isCorrect: true) {
                nil은 모든 서비스, 배열은 지정된 서비스만 발견
                
                @Justification(reaction: "정확합니다! 🎯") {
                    nil은 기기의 모든 서비스를 발견합니다.
                    UUID 배열을 전달하면 해당 서비스만 찾습니다.
                    성능을 위해 필요한 서비스만 지정하는 것을 권장합니다.
                }
            }
            
            @Choice(isCorrect: false) {
                차이 없다
                
                @Justification(reaction: "차이가 있습니다.") {
                    nil은 모든 서비스, 배열은 필터링됩니다.
                }
            }
            
            @Choice(isCorrect: false) {
                배열을 전달하면 에러가 발생한다
                
                @Justification(reaction: "틀렸습니다.") {
                    UUID 배열 전달은 정상적인 사용법입니다.
                }
            }
        }
    }
}
