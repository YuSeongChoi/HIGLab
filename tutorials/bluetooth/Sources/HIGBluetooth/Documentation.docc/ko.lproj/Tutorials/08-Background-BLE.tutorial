@Tutorial(time: 20) {
    @Intro(title: "백그라운드 BLE") {
        앱이 백그라운드에서도 BLE 통신을
        유지하는 방법을 구현합니다.
    }
    
    @Section(title: "백그라운드 모드 설정") {
        @ContentAndMedia {
            iOS에서 백그라운드 BLE를 사용하려면
            Info.plist에 백그라운드 모드를 추가해야 합니다.
            
            **백그라운드 모드:**
            - `bluetooth-central`: Central 역할
            - `bluetooth-peripheral`: Peripheral 역할
            
            App Store 제출 시 정당한 사용 사유가 필요합니다.
        }
        
        @Steps {
            @Step {
                Info.plist에 백그라운드 모드를 추가합니다.
                
                @Code(name: "BackgroundModes.swift", file: "08-01-background-modes.swift")
            }
            
            @Step {
                Xcode의 Signing & Capabilities에서도 설정할 수 있습니다.
                
                @Code(name: "CapabilitiesSetup.swift", file: "08-02-capabilities.swift")
            }
        }
    }
    
    @Section(title: "상태 복원 (State Restoration)") {
        @ContentAndMedia {
            앱이 시스템에 의해 종료되어도
            BLE 연결을 복원할 수 있습니다.
            
            CBCentralManager 생성 시 restoration identifier를 지정하면
            시스템이 연결 상태를 유지합니다.
        }
        
        @Steps {
            @Step {
                State Restoration을 위한 옵션을 설정합니다.
                
                @Code(name: "BluetoothManager.swift", file: "08-03-restoration-id.swift")
            }
            
            @Step {
                앱 재시작 시 복원 콜백을 처리합니다.
                
                @Code(name: "BluetoothManager.swift", file: "08-04-will-restore.swift")
            }
            
            @Step {
                복원된 Peripheral 상태를 처리합니다.
                
                @Code(name: "BluetoothManager.swift", file: "08-05-handle-restored.swift")
            }
        }
    }
    
    @Section(title: "백그라운드 스캔") {
        @ContentAndMedia {
            백그라운드에서 스캔하면 제한이 있습니다.
            
            **백그라운드 스캔 제한:**
            - 반드시 서비스 UUID를 지정해야 함
            - 스캔 간격이 길어짐
            - `allowDuplicates` 옵션 무시됨
        }
        
        @Steps {
            @Step {
                백그라운드 호환 스캔을 구현합니다.
                
                @Code(name: "BluetoothManager.swift", file: "08-06-background-scan.swift")
            }
            
            @Step {
                앱 상태에 따라 스캔 전략을 변경합니다.
                
                @Code(name: "BluetoothManager.swift", file: "08-07-adaptive-scan.swift")
            }
        }
    }
    
    @Section(title: "백그라운드 알림") {
        @ContentAndMedia {
            백그라운드에서 BLE 이벤트 발생 시
            로컬 알림으로 사용자에게 알립니다.
        }
        
        @Steps {
            @Step {
                백그라운드 기기 발견 시 알림을 표시합니다.
                
                @Code(name: "BackgroundNotification.swift", file: "08-08-local-notification.swift")
            }
        }
    }
    
    @Assessments {
        @MultipleChoice {
            백그라운드에서 BLE 스캔 시 반드시 필요한 것은?
            
            @Choice(isCorrect: true) {
                serviceUUIDs 지정
                
                @Justification(reaction: "정확합니다! 🎯") {
                    백그라운드에서는 nil로 모든 기기 스캔이 불가합니다.
                    관심있는 서비스 UUID를 명시해야 합니다.
                }
            }
            
            @Choice(isCorrect: false) {
                allowDuplicates: true
                
                @Justification(reaction: "백그라운드에서 무시됩니다.") {
                    이 옵션은 백그라운드에서 자동으로 false로 처리됩니다.
                }
            }
            
            @Choice(isCorrect: false) {
                특별한 요구사항 없음
                
                @Justification(reaction: "제한이 있습니다.") {
                    serviceUUIDs가 필수이고 스캔 간격도 제한됩니다.
                }
            }
        }
        
        @MultipleChoice {
            State Restoration이 필요한 이유는?
            
            @Choice(isCorrect: true) {
                앱이 시스템에 의해 종료되어도 연결을 복원하기 위해
                
                @Justification(reaction: "맞습니다! 🔄") {
                    메모리 부족 등으로 앱이 종료되어도
                    시스템이 BLE 연결을 유지하고,
                    앱 재시작 시 복원해줍니다.
                }
            }
            
            @Choice(isCorrect: false) {
                배터리 절약을 위해
                
                @Justification(reaction: "직접적인 목적은 아닙니다.") {
                    연결 복원이 주 목적입니다.
                }
            }
            
            @Choice(isCorrect: false) {
                스캔 속도 향상을 위해
                
                @Justification(reaction: "스캔과 관련 없습니다.") {
                    연결 상태 복원이 목적입니다.
                }
            }
        }
        
        @MultipleChoice {
            CBCentralManagerOptionRestoreIdentifierKey의 값으로 적절한 것은?
            
            @Choice(isCorrect: false) {
                nil
                
                @Justification(reaction: "고유 문자열이 필요합니다.") {
                    nil이면 State Restoration이 비활성화됩니다.
                }
            }
            
            @Choice(isCorrect: true) {
                앱별 고유 문자열 (예: "com.myapp.bluetooth")
                
                @Justification(reaction: "정확합니다! ✨") {
                    앱 내에서 고유한 문자열이면 됩니다.
                    여러 CentralManager를 사용하면 각각 다른 ID가 필요합니다.
                }
            }
            
            @Choice(isCorrect: false) {
                UUID().uuidString (매번 새로 생성)
                
                @Justification(reaction: "고정된 값이어야 합니다.") {
                    매번 다른 값이면 복원이 불가능합니다.
                }
            }
        }
    }
}
