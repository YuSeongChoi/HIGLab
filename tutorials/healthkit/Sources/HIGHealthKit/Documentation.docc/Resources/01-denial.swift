import HealthKit

// MARK: - 권한 거부 시 무응답 정책

/*
 Apple의 프라이버시 보호 정책:
 
 사용자가 읽기 권한을 거부하면, 앱은 이를 알 수 없습니다.
 API는 단순히 "데이터 없음"을 반환합니다.
 
 이것은 버그가 아니라 의도된 설계입니다.
 
 예시 시나리오:
 1. 사용자가 걸음 수 읽기 권한 거부
 2. 앱에서 걸음 수 쿼리
 3. 결과: 빈 배열 [] 반환
 
 앱은 다음 두 경우를 구분할 수 없습니다:
 - 실제로 걸음 수 데이터가 없는 경우
 - 사용자가 권한을 거부한 경우
 
 이로 인해 앱은 사용자의 권한 선택을 추론할 수 없고,
 사용자의 프라이버시가 보호됩니다.
 */

// ✅ 올바른 접근: 데이터 유무와 관계없이 graceful하게 처리
func fetchSteps() async -> Int {
    // 결과가 없으면 0 또는 기본값 표시
    // "권한이 없습니다" 같은 메시지는 피하세요
    return 0
}
