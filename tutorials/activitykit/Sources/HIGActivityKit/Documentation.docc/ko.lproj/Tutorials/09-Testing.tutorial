@Tutorial(time: 10) {
    @Intro(title: "Live Activity 테스트") {
        시뮬레이터와 실제 기기에서 Live Activity를 테스트하는 방법을 학습합니다.
    }
    
    @Section(title: "시뮬레이터 테스트") {
        @ContentAndMedia {
            Xcode 시뮬레이터에서 Live Activity를 테스트합니다.
            Dynamic Island가 있는 기기 시뮬레이터를 사용하세요.
        }
        
        @Steps {
            @Step {
                Push 알림 테스트를 위한 APNs 파일을 생성합니다.
                
                @Code(name: "TestPayload.apns", file: "09-apns-test.swift")
            }
            
            @Step {
                xcrun 명령어로 Push 알림을 시뮬레이터에 전송합니다.
                
                @Code(name: "Terminal", file: "09-simctl-push.swift")
            }
        }
    }
    
    @Section(title: "디버그 로깅") {
        @ContentAndMedia {
            Activity 상태 변화를 로깅하여 디버깅합니다.
        }
        
        @Steps {
            @Step {
                Activity 상태를 모니터링하는 디버그 코드를 추가합니다.
                
                @Code(name: "DebugHelper.swift", file: "09-debug-logging.swift")
            }
        }
    }
    
    @Assessments {
        @MultipleChoice {
            시뮬레이터에서 Live Activity Push 테스트에 사용하는 명령어는?
            
            @Choice(isCorrect: false) {
                xcodebuild push
                
                @Justification(reaction: "다시 생각해보세요.") {
                    xcodebuild는 빌드 도구입니다.
                }
            }
            
            @Choice(isCorrect: true) {
                xcrun simctl push
                
                @Justification(reaction: "정답입니다! 🎉") {
                    simctl push 명령으로 시뮬레이터에 푸시를 전송합니다.
                }
            }
            
            @Choice(isCorrect: false) {
                swift push
                
                @Justification(reaction: "아쉽네요.") {
                    swift 명령어에는 push 옵션이 없습니다.
                }
            }
        }
    }
}
